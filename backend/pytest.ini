[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts =
    --cov=app
    --cov-report=term
    --cov-report=xml:coverage.xml
    --cov-fail-under=70
    -v
filterwarnings =
    ignore::DeprecationWarning
