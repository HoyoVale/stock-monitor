name: Release

on:
  push:
    tags:
      - 'v*'
    branches:
      - 'release/v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version
        id: ver
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            V="${GITHUB_REF#refs/tags/v}"
            echo "trigger=tag" >> $GITHUB_OUTPUT
          else
            V="${GITHUB_REF#refs/heads/release/v}"
            echo "trigger=branch" >> $GITHUB_OUTPUT
          fi
          echo "version=$V" >> $GITHUB_OUTPUT

      - name: Create git tag
        if: steps.ver.outputs.trigger == 'branch'
        run: |
          git config user.name "Release Bot"
          git config user.email "release@github.com"
          git tag -a "v${{ steps.ver.outputs.version }}" -m "v${{ steps.ver.outputs.version }}"
          git push origin "v${{ steps.ver.outputs.version }}"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build frontend
        run: cd frontend && npm ci && npm run build

      - name: Generate release body
        run: |
          V="${{ steps.ver.outputs.version }}"
          cat > release.md << EOF
          # stock-monitor v${V}

          ## 快速开始

          \`\`\`bash
          docker run -d -p 8000:8000 ghcr.io/${{ github.repository_owner }}/stock-monitor-backend:v${V}
          docker run -d -p 80:80 ghcr.io/${{ github.repository_owner }}/stock-monitor-frontend:v${V}
          \`\`\`

          📖 完整使用说明见 [用户手册](https://github.com/${{ github.repository }}/blob/master/docs/USER_GUIDE.md)

          ## 更新日志
          EOF
          grep -A 200 "^## \[${V}\]" CHANGELOG.md | sed '/^## \[0\./q' | head -n -1 >> release.md 2>/dev/null || echo "v${V}" >> release.md
          cat release.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ steps.ver.outputs.version }}
          body_path: release.md
          generate_release_notes: true
          files: frontend/dist/**/*
